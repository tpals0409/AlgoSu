---
# Sealed Secrets 템플릿 — 서비스별 Secret 암호화
#
# 사용법:
# 1. kubeseal CLI 설치: brew install kubeseal
# 2. 원본 Secret 작성 → kubeseal로 암호화 → 암호화된 YAML만 Git 커밋
# 3. 원본 Secret 파일은 절대 Git에 커밋 금지
#
# 예시:
#   kubectl create secret generic gateway-secrets \
#     --from-literal=JWT_SECRET=your_real_secret \
#     --from-literal=REDIS_URL=redis://redis:6379 \
#     --from-literal=INTERNAL_KEY_IDENTITY=key1 \
#     --from-literal=INTERNAL_KEY_PROBLEM=key2 \
#     --from-literal=INTERNAL_KEY_SUBMISSION=key3 \
#     --from-literal=INTERNAL_KEY_AI_ANALYSIS=key4 \
#     --dry-run=client -o yaml | kubeseal -o yaml > sealed-gateway-secrets.yaml

# ── Gateway Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: gateway-secrets
  namespace: algosu
spec:
  encryptedData:
    JWT_SECRET: "<sealed>"
    JWT_EXPIRES_IN: "<sealed>"
    REDIS_URL: "<sealed>"
    INTERNAL_KEY_IDENTITY: "<sealed>"
    INTERNAL_KEY_PROBLEM: "<sealed>"
    INTERNAL_KEY_SUBMISSION: "<sealed>"
    INTERNAL_KEY_AI_ANALYSIS: "<sealed>"
    IDENTITY_SERVICE_URL: "<sealed>"
    PROBLEM_SERVICE_URL: "<sealed>"
    SUBMISSION_SERVICE_URL: "<sealed>"
    AI_ANALYSIS_SERVICE_URL: "<sealed>"

---
# ── Submission Service Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: submission-service-secrets
  namespace: algosu
spec:
  encryptedData:
    DATABASE_HOST: "<sealed>"
    DATABASE_PORT: "<sealed>"
    DATABASE_NAME: "<sealed>"
    DATABASE_USER: "<sealed>"
    DATABASE_PASSWORD: "<sealed>"
    REDIS_URL: "<sealed>"
    RABBITMQ_URL: "<sealed>"
    INTERNAL_API_KEY: "<sealed>"
    PROBLEM_SERVICE_URL: "<sealed>"
    PROBLEM_SERVICE_KEY: "<sealed>"

---
# ── Problem Service Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: problem-service-secrets
  namespace: algosu
spec:
  encryptedData:
    DATABASE_HOST: "<sealed>"
    DATABASE_PORT: "<sealed>"
    DATABASE_NAME: "<sealed>"
    DATABASE_USER: "<sealed>"
    DATABASE_PASSWORD: "<sealed>"
    REDIS_URL: "<sealed>"
    INTERNAL_API_KEY: "<sealed>"
    INTERNAL_KEY_GATEWAY: "<sealed>"
    GATEWAY_INTERNAL_URL: "<sealed>"
    DUAL_WRITE_MODE: "<sealed>"
    NEW_DATABASE_HOST: "<sealed>"
    NEW_DATABASE_PORT: "<sealed>"
    NEW_DATABASE_NAME: "<sealed>"
    NEW_DATABASE_USER: "<sealed>"
    NEW_DATABASE_PASSWORD: "<sealed>"

---
# ── Postgres Problem Secret ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-problem-secret
  namespace: algosu
spec:
  encryptedData:
    password: "<sealed>"

---
# ── Identity Service Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: identity-service-secrets
  namespace: algosu
spec:
  encryptedData:
    DATABASE_HOST: "<sealed>"
    DATABASE_PORT: "<sealed>"
    DATABASE_NAME: "<sealed>"
    DATABASE_USER: "<sealed>"
    DATABASE_PASSWORD: "<sealed>"
    INTERNAL_API_KEY: "<sealed>"

---
# ── GitHub Worker Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: github-worker-secrets
  namespace: algosu
spec:
  encryptedData:
    RABBITMQ_URL: "<sealed>"
    REDIS_URL: "<sealed>"
    GITHUB_APP_ID: "<sealed>"
    GITHUB_APP_PRIVATE_KEY_BASE64: "<sealed>"
    GITHUB_APP_INSTALLATION_ID: "<sealed>"
    SUBMISSION_SERVICE_URL: "<sealed>"
    SUBMISSION_SERVICE_KEY: "<sealed>"

---
# ── AI Analysis Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ai-analysis-secrets
  namespace: algosu
spec:
  encryptedData:
    RABBITMQ_URL: "<sealed>"
    REDIS_URL: "<sealed>"
    GEMINI_API_KEY: "<sealed>"
    SUBMISSION_SERVICE_URL: "<sealed>"
    SUBMISSION_SERVICE_KEY: "<sealed>"
    INTERNAL_API_KEY: "<sealed>"

---
# ── Monitoring Secrets ──
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: monitoring-secrets
  namespace: algosu
spec:
  encryptedData:
    GRAFANA_ADMIN_PASSWORD: "<sealed>"
