# AlgoSu — Cloudflare Tunnel (Zero Trust)
# TLS 종료 + 외부 노출을 Cloudflare에 ��임
# 토큰: kubectl secret "cloudflared-token" (Git 커밋 금지)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: algosu
  labels:
    app: cloudflared
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared
  template:
    metadata:
      labels:
        app: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            # --metrics 명시 필수 — liveness probe(:2000/ready)와 포트 일치시키지 않으면
            # cloudflared가 랜덤 포트를 잡아 probe 실패 → CrashLoopBackOff (Sprint 69 G1 참조)
            - --metrics
            - 0.0.0.0:2000
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - $(TUNNEL_TOKEN)
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflared-token
                  key: token
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            initialDelaySeconds: 10
            periodSeconds: 30
          securityContext:
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
